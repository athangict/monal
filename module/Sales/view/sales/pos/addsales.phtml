<?php
/**
 * 
 * View -- of Accounts/AssetController/addtransactionAction
 * 
 */
?>
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-beta.1/dist/css/select2.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-beta.1/dist/js/select2.min.js"></script>
<style>
    .input-cell {
        max-width: 100px; /* Adjust the value as per your requirement */
    }
	<style>
	.mytable tr th,.mytable tr td {
		padding:5px;
	}
</style>
<div class="row">
	<div class="col-lg-12">
        <div class="widget widget-box">
            <div class="widget-header">
            	<h5 class="widget-title lighter smaller">
				Sales Entry
				</h5>
	             </h5>
            </div>
            <div class="widget-body">
				<div class="widget-main">
				<div id="report-print">
					<form  id="itemForm" method="post" action="<?php echo $this->url('pos', array('action' => 'addsales')); ?>"  enctype="multipart/form-data" >   
					<div class="row">
						<div class="form-group col-lg-4">
							<label for="sales_date" class="control-label">Sales Date</label>
							<div class="date input-group" id="sales_date" data-date="<?php echo date('Y-m-d');?>" data-date-format="yyyy-mm-dd" >
								<input class="form-control span2" name="sales_date" id="sales_date" type="text" value="<?php echo date('Y-m-d');?>" placeholder="Sales Date" readonly required>
								<span class="input-group-addon add-on"><i class="fa fa-calendar"></i></span>
							</div>
						</div>
						<div class="form-group col-lg-4">
							<label for="location" class="control-label">Location </label>
							<select class="form-control" name="location" id="location" data-placeholder="Select" required >
								<option value=""></option>
								<?php foreach($this->locationObj->get(array('id'=>$admin_location)) as $location):?>
									<option value="<?php echo $location['id'];?>" <?php echo ($source_locs == $location['id'])?'selected':'';?>>
										<?php echo $location['location'];?>
									</option>
								<?php endforeach; ?>
							</select>
						</div>
						<div class="form-group col-lg-4">
							<label for="sales_person" class="control-label">Sales Person</label>
							<select class="form-control" name="sales_person" id="sales_person" data-placeholder="Select" required >
								<option value=""></option>
									<?php foreach($employees as $employee):?>
									<option value="<?php echo $employee['id'];?>"<?php echo 'selected';?>>
										<?php echo $employee['name'];?>
									</option>
								<?php endforeach;?>
							</select>
						</div>
						<div class="form-group col-lg-4" id="payment">
							<label for="payment_type" class="control-label">Payment Mode</label>
							<select class="form-control" name="payment_type" id="payment_type" data-placeholder="Select">
								
								<option value=0>None</option>
								<option value=1>Cash</option>
								<option value=2>Bank</option>
							</select>
						</div>
						<div class="form-group col-lg-4" id="account">
							<label for="account_no" class="control-label">Account No.</label>
							<select class="form-control" name="account_no" id="account_no" data-placeholder="Select" required>
								<option value=0></option>
							</select>
						</div>
						<div class="form-group col-lg-4" id="journel"style="display:none;">
							<label for="jrnl_no" class="control-label">Journel No.</label>
							<input type="text" class="form-control" name="jrnl_no" id="jrnl_no" value="" placeholder="Journel No">
						</div>
						<div class="form-group col-lg-4" id="mobile" style="display:none;">
							<label for="phone" class="control-label">Phone No.</label>
							<input type="text" class="form-control" name="phone" id="phone"  value="0" placeholder="Phone No" >
						</div>
						<div class="form-group col-lg-4">
							<label for="credit" class="control-label">Credit/Advance</label>
							<div class="radio">
								<label>  
									<input class="ace" type="radio" name="credit" id="credit"  value="y"/>
									<span class="lbl"> Credit </span>
								</label> 
								<label>  
									<input class="ace" type="radio" name="credit" id="credit" value="1"/>
									<span class="lbl"> Advance </span>
								</label> 
								<label> 
									<input class="ace" type="radio" name="credit" id="credit" value="n" checked/> 
									<span class="lbl"> No </span>
								</label>
							</div>
						</div>
						<div class="form-group col-lg-4" id="customers" style="display:none;">
							<label for="customer" class="control-label">Customer</label>
							<select class="form-control" name="customer" id="customer" data-placeholder="Select" >
								<option value="0">Nil</option>
							</select>
						</div>
						<div class="form-group col-lg-4" id="due_dates" style="display:none;">
							<label for="sales_date" class="control-label">Due Date</label>
							<div class="date input-group" id="due_date" data-date="<?php echo date('Y-m-d');?>" data-date-format="yyyy-mm-dd" >
								<input class="form-control span2" name="due_date" id="due_date" type="text" value="<?php echo date('Y-m-d');?>" placeholder="Sales Date" required>
								<span class="input-group-addon add-on"><i class="fa fa-calendar"></i></span>
							</div>
						</div>
						<div class="form-group col-lg-4" id="ref_no" style="display:none;">
							<label for="ref_no" class="control-label">Reference No</label>
							<input type="text" class="form-control" name="ref_no" id="ref_no" placeholder="Reference No" >
						</div>
						<div class="form-group col-lg-4">
							<label for="discount" class="control-label">Discount</label>
							<select class="form-control" name="discount" id="discount" data-placeholder="Select" required >
								<option value="0">0.00</option>
									<option value="15">15%</option>
									<option value="20">20%</option>
									<option value="100">100%</option>
							</select>
						</div>
					</div>
					<div class="row">
						<div class="col-lg-12"> 
							<h3>Item Details</h3>
							<div>
							<table id="item_field"class="table small-padding">
							<tbody>
								<tr>
									<td class="col-lg-3">
										<div class="form-group">
										<label class="control-label" for="item_group">Item Group</label>
											<select  class="form-control" name="item_subgroup" id="item_subgroup" data-placeholder="Select">
												<option value=""></option>
												<?php foreach($itemgroups as $itemgroup):?>
													<option value="<?php echo $itemgroup['id']; ?>">
														<?php echo $itemgroup['item_group']; ?>
													</option>
												<?php endforeach; ?>
											</select>
										</div>
									</td>
									<td class="col-lg-2 ">
										<div class="form-group">
										<label for="item" class="control-label">Item</label>
											<select class="form-control" name="item[]" id="item" data-placeholder="Select"  readonly>
												<option value=""></option>
											</select> 
										</div>
									</td>
									<td class="col-lg-2 input-cell">
										<div class="form-group">
										<label for="quantity" class="control-label">Quantity</label>
											<input type="text" class="form-control tr_quantity"  name="quantity[]" id="quantity" value="" >
										</div>
									</td>
									<td class="col-lg-2 input-cell">
										<div class="form-group">
											<label for="basic_uom" class="control-label">Unit of Measurement</label>
											<input type="text" class="form-control tr_basic_uom"  name="basic_uom[]" id="basic_uom" value=" " readonly>
										</div>
									</td>
									<td class="col-lg-1">
										<div class="form-group">
											<label for="selling_price" class="control-label">Rate</label>
											<input type="text" class="form-control tr_rate"  name="rate[]" id="rate" value="0.00" readonly>
										</div>         
									</td>
									<td class="col-lg-1">
										<div class="form-group">
											<label for="stock_qty" class="control-label">Stock Qty</label>
											<input type="text" class="form-control tr_stock_qty"  name="stock_qty[]" id="stock_qty" value="0.00" readonly>
										</div>         
									</td>
									<td class="col-lg-1">
										<div class="form-group">
											<label for="amount" class="control-label">Total</label>
											<input type="text" class="form-control tr_amount"  name="amount[]" id="amount" value="0.00" readonly>
										</div>         
									</td>
									<!-- <td>
										<br>
										<button class="btn btn-sm btn-success" type="button"  onclick="item_fields();"> <span class="fa fa-plus-square" aria-hidden="true"></span> </button>
									</td> -->
								</tr> 
							</tbody>
							<table>
							<div class="row"> 
								<div class="col-lg-11"></div>
								<button class="btn btn-xs btn-success" type="button"  onclick="item_fields();"> <span class="fa fa-plus-square" aria-hidden="true"> Add Row</span> </button>
							</div>
								<div class="clear"></div>
							</div>
						</div>
					</div>
					<div class="row">
						<div class="col-lg-6"></div>
						<div class="col-lg-3">
							<div class="form-group">
								<label for="total">Total</label>
								<input type="text" class="form-control" name="total" id="total" value="0.000" readonly />
							</div>
						</div> 
						<div class="col-lg-3">
							<div class="form-group">
								<label for="net_amount">Net Amount</label>
								<input type="text" class="form-control" name="net_amount" id="net_amount"  value="0.000" readonly />
							</div>
						</div>
					</div>
						<br/>
						<button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Save </button>
						<button type="button" class="btn btn-warning" onclick="javascript:history.back();" ><i class="fa fa-times"></i> Cancel </button> 
					</form>
				</div>
			</div>
		</div>
	</div>
</div>
<br/>
<script>
	var room = 1;
	function item_fields() {
    room++;
    var objTo = document.getElementById('item_field');
    var divtest = document.createElement('tr');
    divtest.setAttribute('class', 'form-group removeclass' + room);
    
    divtest.innerHTML = 
		'<tr><td class="col-lg-3"><div class="form-group"><select name="item_subgroup[]" class="form-control tr_item_subgroup" data-placeholder="Select" required><option value="-1">Select<?php foreach($itemgroups as $itemgroup):?><option value="<?php echo $itemgroup['id']; ?>"><?php echo $itemgroup['item_group']; ?></option><?php endforeach; ?></select></div></td><td class="col-lg-2 "><div class="form-group"><select class="form-control tr_item" name="item[]" id="item" data-placeholder="Select" required readonly><option value=""></option>	</select></div></td><td class="col-lg-1"><div class="form-group"><input type="text" class="form-control tr_quantity" id="quantity" name="quantity[]"  value=""></div></td><td class="col-lg-1"><div class="form-group"><input type="text" class="form-control tr_basic_uom" id="basic_uom" name="basic_uom[]"  value="" readonly></div></td><td class="col-lg-1"><div class="form-group"><input type="text" class="form-control tr_rate" id="rate" name="rate[]"  value="" readonly></div></td><td class="col-lg-1"><div class="form-group"><input type="text" class="form-control tr_stock_qty" id="stock_qty" name="stock_qty[]"  value="" readonly></div></td><td class="col-lg-1"><div class="form-group"><input type="text" class="form-control tr_amount" id="amount" name="amount[]"  value="" readonly></div></td><td class="col-lg-1"><div class="input-group-btn"><button class="btn btn-sm btn-danger" type="button" onclick="remove_item_fields(' + room + ');"><span class="glyphicon-minus" aria-hidden="true"></span></button></div></td><tr>';
		objTo.appendChild(divtest);
		calculateTotalAmount();
		calculateTotalNet();
		$(divtest).find("#item").select2();
	}
	function remove_item_fields(rid) {
	$('.removeclass' + rid).remove();
	calculateTotalAmount();
	calculateTotalNet();
	}
	$(document).ready(function() {
    // Initialize select2 on all current "item" dropdowns
	var relatedItemElement = $(this).closest('tr').find('.tr_item');
	relatedItemElement.select2({
        placeholder: "Select an item",
        allowClear: true
    });
});
	// Use the document as the parent and delegate the event to the .tr_item_subgroup class
	$(document).on('change', '.tr_item_subgroup', function() {
		var selectedItemSubgroup = $(this).val(); // get the value of the changed .tr_item_subgroup
		var relatedItemElement = $(this).closest('tr').find('.tr_item'); // find the related .tr_item in the same row
		
		// Assuming you want to do an AJAX call similar to what you've shown in your initial code:
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getItemChange')); ?>",
			{
				locationId: $("#location").val(),
				item_subgroup: selectedItemSubgroup,
			},
			function(data) {
				relatedItemElement.html(data.items); // updating the related .tr_item
				relatedItemElement.trigger('chosen:updated'); // if you're using chosen plugin
				
			},
			'json'
		);  
	});
	
	$(document).on('change', '.tr_item', function() {
		var selectedItem = $(this).val(); // get the value of the changed .tr_item_subgroup
		var relatedItemElement = $(this).closest('tr').find('.tr_rate'); // find the related .tr_item in the same row
		var relatedItemElement1 = $(this).closest('tr').find('.tr_stock_qty');
		var relatedItemElement2 = $(this).closest('tr').find('.tr_cost_price');
		// Assuming you want to do an AJAX call similar to what you've shown in your initial code:
		$.post(
				"<?php echo $this->url('pos', array('action' => 'getrate')); ?>",
				{
					item:selectedItem,
					location:$('#location').val(),
				},
				function(data){
					console.log(data);
					relatedItemElement.val(data.rate);
					relatedItemElement1.val(data.quantity);	
					relatedItemElement2.val(data.cost_price);	
				},
				'json'
			);	
	});
	
	$(document).on('change', '.tr_item', function() {
		var selectedItem = $(this).val(); // get the value of the changed .tr_item_subgroup
		var relatedItemElement = $(this).closest('tr').find('.tr_basic_uom'); // find the related .tr_item in the same row
		
		// Assuming you want to do an AJAX call similar to what you've shown in your initial code:
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getuom')); ?>",
			{
				itemId: selectedItem,
			},
			function(data) {
				relatedItemElement.val(data.uom); // updating the related .tr_item
				
			},
			'json'
		);  
	});
	$(document).on('input', '.tr_rate, .tr_quantity', function() {
		calculateTotal($(this));
	});
	$(document).on('input', '.tr_amount,.tr_quantity', function () {
		calculateTotalAmount();
		calculateTotalNet();
		});
	function calculateTotal(element) {
		var row = element.closest('tr');  // Get the closest table row

		var rateInput = row.find('.tr_rate');  // Adjusted these to use class selectors and scope within the row
		var quantityInput = row.find('.tr_quantity');
		var totalInput = row.find('.tr_amount'); // Assuming there's a field for the total amount with the class "tr_amount"

		var rate = parseFloat(rateInput.val()) || 0;
		var quantity = parseFloat(quantityInput.val()) || 0;
		var total = rate * quantity;

		totalInput.val(total.toFixed(2));  // Assuming you want to show up to two decimal places
	}
	$(document).on('input', '.tr_amount', function() {
		calculateTotalAmount();
		calculateTotalNet();
	});
	
	function calculateTotalAmount() {
		console.log("calculateTotal amount");
		var total = 0.0;
		// Iterate over each amount input field in the table
		$('.tr_amount').each(function() {
			var amount = parseFloat($(this).val());
			if (!isNaN(amount)) { // Make sure it's a valid number
				total += amount;
			}
		});

		// Update the #total field with the new sum
		$('#total').val(total.toFixed(2));
	}		
	function calculateTotalNet() {
		var total = 0.0;
		var discount=$('#discount').val();
		// Iterate over each amount input field in the table
		$('.tr_amount').each(function() {
			var amount = parseFloat($(this).val());
			if(discount!=0 && !isNaN(amount)){
				total += amount-(amount*(discount/100));
			}
			else if (!isNaN(amount)) { // Make sure it's a valid number
				total += amount;
			}
		});
		// Update the #total field with the new sum
		$('#net_amount').val(total.toFixed(2));
	}	
	$(document).on('input', '.tr_quantity', function() {
		// Fetch the entered quantity from the changed input
		var enteredQuantity = parseFloat($(this).val()) || 0;
		
		// Find the related stock quantity in the same row
		var stockQtyElement = $(this).closest('tr').find('.tr_stock_qty');
		var stockQty = parseFloat(stockQtyElement.val()) || 0;

		// Check if entered quantity exceeds stock quantity
		if (stockQty-enteredQuantity<0 ) {
			alert('Entered quantity exceeds available stock!');
			$(this).val(stockQty); // Reset to max available quantity (optional)
			// You might also want to add visual cues, like changing the background color
			$(this).css('background-color', '#FFCCCC');
		} else {
			$(this).css('background-color', ''); // Reset background color
		}

		calculateTotal($(this)); 
	});
	
$(document).ready(function (){
	$('#item,#location').on('change', function(){
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getrate')); ?>",
			{
				item:$('#item').val(),
                location:$('#location').val(),
			},
			function(data){
				console.log(data);
				$("#rate").val(data.rate);
                $("#stock_qty").val(data.quantity);	
				$("#cost_price").val(data.cost_price);	
			},
			'json'
		);	
	});
	$('#item_subgroup,#location').on('change', function(){
		//alert($(this).val());
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getItemChange')); ?>",
			{
				locationId: $("#location").val(),
				item_subgroup: $("#item_subgroup").val(),
			},
			function(data){
				console.log(data);
				$("#item").html(data.items);	
				$("#item").trigger('chosen:updated');
			},
			'json'
		);	
	});
	//check if the entered quantity exceeds the current stock quantity
	/*$('#quantity').on('change', function(){
		if($('#stock_qty').val()-$('#quantity').val()<0){
			alert('Entered quantity exceeds available stock!');
			$('#quantity').css('background-color', '#FFCCCC');
		} else {
			$('#quantity').css('background-color', ''); // Reset background color
		}
	});*/
	
	$('Select#item').on('change', function(){
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getuom')); ?>",
			{
				itemId: $(this).val(),
			},
			function(data){
				console.log(data);
				$("#basic_uom").val(data.uom);
			},
			'json'
		);	
	});
	
	var rateInput = $('#rate');
	var quantityInput = $('#quantity');
  // Add an event listener for changes in the rate and quantity inputs
	rateInput.on('input', calculateTotal);
	quantityInput.on('input', calculateTotal);

  // Function to calculate the total and update the total input field
	function calculateTotal() {
		var rate = parseFloat(rateInput.val());
		var quantity = parseFloat(quantityInput.val());	
		var total = rate * quantity;
    // Check if the total is a valid number
    if (!isNaN(total)) {
      $('#amount').val(total.toFixed(2)); // Display the total with 2 decimal places
    }
	calculateTotalAmount();
	calculateTotalNet();
  }
  $('input[type=radio][name=credit]').change(function(){
        if ($(this).val() == "y") {
			$('#due_dates').show();
			$('#customers').show();
			$('#payment').hide();
			$('#account').hide();
			$('#ref_no').show();
        } 
		else if($(this).val() == "1"){
			$('#due_dates').hide();
			 $('#customers').show();
			 $('#payment').hide();
			$('#account').hide();
			  $('#ref_no').hide();
			
		}else {
            $('#due_dates').hide();
			$('#customers').hide();
			$('#payment').show();
			$('#account').show();
			 $('#ref_no').hide();
        } 
			$.post(
			"<?php echo $this->url('pos', array('action' => 'getcustomer')); ?>",
			{
				id: $(this).val(),
			},
			function(data){
				console.log(data);
				$("#customer").html(data.customer);	
				$("#customer").trigger('chosen:updated');
			},
			'json'
		);	
    });
    $('#due_date').datepicker();
	$('#sales_date').datepicker();
	
	$('#payment_type,#location').on('change', function(){
		$.post(
			"<?php echo $this->url('pos', array('action' => 'getBCAccount')); ?>",
			{
				paymentId: $('#payment_type').val(),
				locationId: $('#location').val(),
			},
			function(data){
				console.log(data);
				$("#account_no").html(data.account);	
				$("#account_no").trigger('chosen:updated');
			},
			'json'
		);	
		if($('#payment_type').val()==2){
			$('#journel').show();
			$('#mobile').show();

		}
		else{
			$('#journel').hide();
			$('#mobile').hide();
		}
	});
	$('form input, form select').on('keydown', function (e) {
        // Check for the Enter key
        if (e.which === 13) {
            e.preventDefault(); // Prevent form submission

            // Get all input elements in form
            var inputs = $(this).closest('form').find(':input:visible:not([disabled])');

            // Find the next input element
            var nextInput = inputs.eq(inputs.index(this) + 1);
            
            if (nextInput.length) {
                nextInput.focus();
            } else {
                // If there's no next input, you might want to trigger form submission
                // $(this).closest('form').submit(); 
            }
        }
    });
	const fv = FormValidation.formValidation(document.getElementById('itemForm'), {
		fields: {
			'payment_type': {
				validators: {
					notEmpty: {
						message: 'Please select Payment Type.'
					}
				}
			},
			'account_no': {
                validators: {
                    callback: {
					message: 'Select the account no',
					callback: function(input) {
					  return input.value !== '-1';
					},
				  },
                }
            },
			'location': {
				validators: {
					notEmpty: {
						message: 'Please select location.'
					}
				}
			},
			'stock_qty[]': {
                    validators: {
                        callback: {
                            message: 'Not enough stock',
                            callback: function(input) {
                                return parseFloat(input.value) >= 0;
                            },
                        },
                    }
                },
				'quantity[]': {
                    validators: {
                        callback: {
                            message: 'Quantity Cannot Be Negative',
                            callback: function(input) {
                                return parseFloat(input.value) >= 0;
                            },
                        },
                    }
                },
			'customer': {
                validators: {
                    callback: {
					message: 'Select the Customer',
					callback: function(input) {
					  return input.value !== '-1';
					},
				  },
                }
            },
			'sales_person': {
				validators: {
					notEmpty: {
						message: 'Please select Sales Person.'
					}
				}
			},
		},
		plugins: {
			trigger: new FormValidation.plugins.Trigger(),
			bootstrap3: new FormValidation.plugins.Bootstrap3(),
			submitButton: new FormValidation.plugins.SubmitButton(),
			defaultSubmit: new FormValidation.plugins.DefaultSubmit(),
		},
	}).on('core.element.validated', function (e) {
		if (e.valid) {
			$('div#'+ e.field.replace('[]','') +'_chosen').removeClass('is-invalid');
			$('div#'+ e.field.replace('[]','') +'_chosen').addClass('is-valid');
		} else {
			$('div#'+ e.field.replace('[]','') +'_chosen').removeClass('is-valid');
			$('div#'+ e.field.replace('[]','') +'_chosen').addClass('is-invalid');
		}
	});
});
</script>
