<?php
	/**
	 * Stock/SamController/addstockopening	
	 * Add Stock Opening
	**/
	$this->headTitle($this->title);
?>
<div class="row">
	<div class="col-lg-12">
		<div class="widget widget-box">
			<div class="widget-header">
				<h5 class="widget-title lighter smaller">
					<i class="fa fa-pencil"></i>&nbsp;Add New Stock Opening &nbsp;&nbsp;
				</h5>
			</div><!-- end of widget-header -->
			<div class="widget-body">
				<div class="widget-main">
					<form id="#form" method="post" action="<?php echo $this->url('sam', array('action' => 'addstockopening')); ?>"  enctype="multipart/form-data" >    
						<div class="row">
							<div class="col-lg-3 form-group">
								<label for="batch_date" class="control-label">Opening Date</label>
								<div class="date input-group" id="batch_date" data-date="<?php echo date('Y-m-d');?>" data-date-format="yyyy-mm-dd" >
									<input class="form-control span2" value="<?php echo date('Y-m-d');?>" name="batch_date" id="batch_date" type="text" placeholder="Batch Date" readonly required>
									<span class="input-group-addon add-on"><i class="fa fa-calendar"></i></span>
								</div>
							</div>
							<div class="col-lg-3 form-group">
								<label for="issue_date" class="control-label">Issue Date</label>
								<div class="date input-group" id="issue_date" data-date="<?php echo date('Y-m-d');?>" data-date-format="yyyy-mm-dd" >
									<input class="form-control span2" name="issue_date" id="issue_date" type="text" placeholder="Issue Date" >
									<span class="input-group-addon add-on"><i class="fa fa-calendar"></i></span>
								</div>
							</div>
							<div class="col-lg-3 form-group">
								<label class="control-label" for="item_group">Item Group</label>
								<select  class="form-control" name="item_subgroup" id="item_subgroup" data-placeholder="Select" required >
									<option value=""></option>
									<?php foreach ($itemclasses as $itemclass):  
											$itemgroups = $itemgroupsObj->get(array('item_class'=>$itemclass['id']));
											foreach($itemgroups as $itemgroup):
										?>
										<optgroup label="<?php echo $itemclass['item_class']." | ".$itemgroup['item_group'];?>">
											<?php 
												$itemsubgroups = $this->itemsubgroupsObj->get(array('item_group'=>$itemgroup['id']));
												foreach($itemsubgroups as $itemsubgroup):
											?>
												<option value="<?php echo $itemsubgroup['id']; ?>">
													<?php echo $itemsubgroup['item_subgroup']; ?>
												</option>
											<?php endforeach;
											endforeach; ?>
										</optgroup>
									<?php endforeach; ?>
								</select>
							</div>
							<div class="col-lg-3 form-group">
								<label for="item" class="control-label">Item</label>
								<select class="form-control" name="item" id="item" data-placeholder="Select" required>
									<option value=""></option>
									<?php $items = $this->itemObj->getAll();
									foreach($items as $item):?>
									<option value="<?php echo $item['id'];?>"><?php echo $item['name'];?></option>
									<?php endforeach;?>
								</select> 
							</div>
							<div class="col-lg-3 form-group">
								<label for="basic_uom" class="control-label"> UOM</label>
								<select class="form-control" name="basic_uom" id="basic_uom" data-placeholder="Select" required>
									<option value=""></option>				
								</select>
							</div>
							<div class="col-lg-3 form-group">
								<label for="cost_price" class="control-label">Printing Price</label>
								<input type="text" class="form-control"  name="cost_price" id="cost_price" >
							</div>
							<div class="col-lg-3 form-group">
								<label for="selling_price" class="control-label">Selling Price</label>
								<input type="text" class="form-control"  name="selling_price" id="selling_price" >
							</div>
						</div>
						<div class="space-12"></div>
						<div class="row">
							<div class="col-lg-12">
								<table class="table table-striped table-condensed" id="opening_details" style="border-top:2px solid #ddd;">
									<thead>
										<tr>
											<th>Location</th>	
											<th>Quantity in Basic UOM</th>
											<th></th>
										</tr>
									</thead>
									<tbody>
										<tr> 
											<td>
												<div class="form-group">
													<select class="form-control ace" name="location[]" id="location" data-placeholder="Select" required>
														<option value=""></option><?php 
															foreach($locations as $loc):?>
															<option value="<?php echo $loc['id'];?>">
																<?php echo $loc['location'];?>
															</option>
														<?php endforeach;?>	
													</select>
												</div>
											</td>
											<td><div class="form-group"><input type="text" name="quantity[]" id="quantity" class="form-control" value="0.00" required></div></td>
											<td></td>
										</tr>
									</tbody>
								</table>
							</div>
						</div>
						<div class="space-12"></div>
						<div class="row">
							<div class="col-lg-12">
							   <button type="submit" class="btn btn-success"><i class="fa fa-save"></i> &nbsp; Save Opening Details</button>
							</div>
						</div>	
					</form>    
				</div> 
			</div>   
      	</div>
    </div>   
</div>	
					
<script type="text/javascript">                                                          
$(function() {
	$('#batch_date').datepicker(); 	
	$('#issue_date').datepicker(); 	
	$('#batch_date').on('changeDate',function(){
		$('form').formValidation('revalidateField', $(this).attr('id'));
	});	
	$("select#item_subgroup").change(function() {
		$.post(
			"<?php echo $this->url('sam', array('action' => 'getitem'));?>",
			{
				itemsubgroupId: $(this).val(),
			},
			function(data){
				console.log(data);	
				$('select#item').html(data.item);
				$('select#item').trigger('chosen:updated');
				
			},
			'json'
		);	
	});
    $("select#item").change(function() {
		$.post(
			"<?php echo $this->url('sam', array('action' => 'getuom'));?>",
			{
				itemId: $(this).val(),
			},
			function(data){
				console.log(data);	
				$('select#basic_uom').html(data.uom);
				$('select#basic_uom').trigger('chosen:updated');
				//$('select#st_uom').html(data.st_uom);
				//$('select#st_uom').trigger('chosen:updated');
				//$('form').formValidation('revalidateField',$('select#basic_uom'));
				//$('form').formValidation('revalidateField',$('select#st_uom'));
				//$('form').formValidation('revalidateField',$('#batch'));
				
			},
			'json'
		);	
	});
	$('select#item').on('change',function(){
		$.post(
			"<?php echo $this->url('sam', array('action' => 'getuoms'));?>",
			{
				item: $(this).val(),
				batch_date: $('input#batch_date').val(),
			},
			function(data){
				$('select#basic_uom').html(data.basic_uom);
				$('select#basic_uom').trigger('chosen:updated');
				$('select#st_uom').html(data.costingUom);
				$('select#st_uom').trigger('chosen:updated');
				$('#batch').val(data.batch_code);
				
			},
			'json'
		);	
	});
	/** Table **/
	var tr_id =0;
	var parentObj = $('#opening_details');
	parentObj.wrap("<div style='overflow-x:scroll;margin-bottom:5px;'> </div>");
	parentObj.find("tbody tr").each(function(){
		$(this).attr('id',++tr_id);			
		$(this).find('div.chosen-search input').each(function(){
			$(this).addClass('ch-search');
		});
		$(this).find('div.chosen-container .chosen-drop').each(function(){
			$(this).css({'position':'relative'});
		});
		$(this).find(':input').not('input.ch-search').each(function(){
			var old_id = $(this).attr('id');
			var new_id=(old_id.lastIndexOf('-') > 0)? old_id.substr(0, old_id.lastIndexOf('-')): old_id;
			$(this).attr('id',new_id+'-'+tr_id);
			$(this).addClass('tr-'+new_id);
			//$('form').formValidation('addField', $(this));
		});
		$(this).find('select.tr-location').each(function(){
			$(this).bind('change', function(){
				var id = $(this).closest('tr').attr('id');
				var cur_val = $(this).val();
				
				var cur_obj = $('#location-'+id).closest('tr');
				$(document).find('select.tr-location').not('#location-'+id).each(function(){
					if(cur_val == $(this).val()){
						opening_reset(cur_obj);
					}
				});
			});
		});
		opening_delbtn(parentObj);
	});
	if($('#addRow').length == 0){
		var addbutton = "<div class='row'><div class='col-lg-12'><a value='Add' id='addRow' class='pull-right btn btn-success btn-xs' ><i class='fa fa-plus'></i> Add Row</a>&nbsp;&nbsp;&nbsp;&nbsp;</div></div>";
		parentObj.parent().parent().parent().append(addbutton);
	};		
	$("#addRow").click(function(){
		opening_addrow(parentObj);			     
	});	
});
function opening_addrow(obj){
	var lastRow = obj.find("tbody tr:last").attr('id');
	var tmplt_row = obj.find("tbody tr:last").clone(true);
	clone = tmplt_row.clone();
	clone.find('div.chosen-container').each(function(){
		$(this).remove();
	});
	clone.attr('id',++lastRow);
	clone.find(':input').each(function(){
		var old_id = $(this).attr('id');
		var new_id=(old_id.lastIndexOf('-') > 0)? old_id.substr(0, old_id.lastIndexOf('-')): old_id;
		$(this).attr('id',new_id+'-'+lastRow);
		$(this).addClass('tr-'+new_id);
		//$('form').formValidation('addField', $(this));
	});
	$('#'+obj.attr('id')+' tbody').append(clone);
	opening_reset(clone);	        
	$('form select').chosen();//{ allow_single_deselect: true }
	clone.find('div.chosen-container .chosen-drop').each(function(){
		$(this).css({'position':'relative'});
	});
	clone.find('div.chosen-search input').each(function(){
		$(this).addClass('ch-search');
	});
	clone.find('select.tr-location').each(function(){
		$(this).bind('change', function(){
			var id = $(this).closest('tr').attr('id');
			var cur_val = $(this).val();
			var cur_obj = $('#location-'+id).closest('tr');
			$(document).find('select.tr-location').not('#location-'+id).each(function(){
				if(cur_val == $(this).val()){
					opening_reset(cur_obj);
				}
			});
		});
	});
	opening_delbtn(obj);
};	
function opening_delbtn(obj){
	if(obj.find("tbody tr").length > 1){
		delBtn = "<a value='Delete' class='btn btn-warning btn-xs delRow' style='margin-top:5px;' ><i class='fa fa-times'></i></a>";
		obj.find("tbody tr").each(function(){
			$(this).find("td:last").html(delBtn);			
		});
		obj.find("td a.delRow").each(function(){
			$(this).click(function(){opening_delrow($(this).closest('tr'));});	
		});
	}else{
		delBtn = "<a value='Delete' class='btn btn-warning btn-xs resetRow' style='margin-top:5px;' ><i class='fa fa-refresh'></i></a>";
		obj.find("tbody tr").each(function(){
			$(this).find("td:last").html(delBtn);
		});
		obj.find("td a.resetRow").each(function(){
			$(this).click(function(){opening_reset($(this).closest('tr'));});	
		});
	}		
};	
function opening_delrow(obj){
	obj.find(':input[data-fv-field]').each(function() {
		//$('form').formValidation('removeField', $(this));
	});	
	var tr_length = obj.parent().find('tr').length;
	var tbody = obj.parent();
	obj.closest("tr").remove();	
	if(tr_length == 2){
		var delBtn = "<a value='Delete' class='btn btn-warning btn-xs resetRow' style='margin-top:5px;' ><i class='fa fa-refresh'></i></a>";
		tbody.find('tr td:last').html(delBtn);
		tbody.find('tr td:last a.resetRow').each(function(){
			$(this).click(function(){opening_reset($(this).closest("tr"));});
		});
	}
}	
function opening_reset(obj){
	var id = obj.attr('id');
	$('#quantity-'+id).val('0.00');
	$('#selling_price-'+id).val('0.00');
	$('#location-'+id).val('selectedIndex',0).trigger('chosen:updated');
	obj.find(':input:hidden').each(function(){$(this).val('');});
	obj.find(':input[data-fv-field]').each(function() {
		//$('form').formValidation('resetField',$(this));
	});
}
	
const fv = FormValidation.formValidation(document.getElementById('#form'), {
	fields: {
		'batch_date': {
			validators: {
				notEmpty: {
					message: 'Please select batch date.'
				}
			}
		},
		'costing_location': {
			validators: {
				notEmpty: {
					message: 'Please select main location.'
				}
			}
		},
		'activity': {
			validators: {
				notEmpty: {
					message: 'Please select an activity.'
				}
			}
		},
		'item': {
			validators: {
				notEmpty: {
					message: 'Please select an item.'
				}
			}
		},
		'batch': {
			validators: {
				notEmpty: {
					message: 'Please define a batch no'
				}
			}
		},
		'basic_uom':{
			validators: {
				notEmpty: {
					message: 'Please select an item.'
				}
			}
		},
		'st_uom': {
			validators: {
				notEmpty: {
					message: 'Please select standard uom.'
				}
			}
		},
		'location[]': {
			validators: {
				notEmpty: {
					message: 'Please select location.'
				}
			}
		},
	},
	plugins: {
		trigger: new FormValidation.plugins.Trigger(),
		bootstrap3: new FormValidation.plugins.Bootstrap3(),
		submitButton: new FormValidation.plugins.SubmitButton(),
		defaultSubmit: new FormValidation.plugins.DefaultSubmit(),
	},
}).on('core.element.validated', function (e) {
	if (e.valid) {
		$('div#'+ e.field.replace('[]','') +'_chosen').removeClass('is-invalid');
		$('div#'+ e.field.replace('[]','') +'_chosen').addClass('is-valid');
	} else {
		$('div#'+ e.field.replace('[]','') +'_chosen').removeClass('is-valid');
		$('div#'+ e.field.replace('[]','') +'_chosen').addClass('is-invalid');
	}
});
</script>